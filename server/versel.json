{
  "name": "test-api",
  "version": 1,
  "runtime": "nodejs/18",
  "build": {
    "cmd": "npm install && npm run start"
  },
  "routes": [
    {
      "src": "/api/*",
      "dest": "index.js"
    }
  ]
}
